{
  "name": "fleetstudio",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install:all": "npm install && cd client && npm install && cd ../server && npm install",
    "start:all": "concurrently \"npm run dev --prefix client\" \"npm run serve --prefix server\""
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
